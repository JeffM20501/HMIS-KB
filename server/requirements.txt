# ============================================================
# Django core / DRF / auth
# ============================================================
Django==6.0.7
asgiref==3.11.1
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0
django-extensions==4.1
dj-database-url==3.1.2
PyJWT==2.13.0
bcrypt==5.0.0
sqlparse==0.5.5

# ============================================================
# Database driver — pick ONE.
# Your original file had FOUR (psycopg, psycopg-binary,
# psycopg-pool, psycopg2-binary). Your settings.py only calls
# plain dj_database_url.config() with no pooling/async setup,
# so psycopg-pool has nothing to attach to, and having both v2
# and v3 drivers installed is pure redundancy — Django uses
# whichever it finds. Keeping psycopg3's binary build (the
# modern default Django 6 documentation points to); if you're
# intentionally still on psycopg2 elsewhere, swap this one line
# for psycopg2-binary==2.9.12 instead — just keep only one.
# ============================================================
psycopg[binary]==3.3.4

# ============================================================
# Media / email / misc integrations already in use
# ============================================================
cloudinary==1.45.0
pillow==12.3.0
python-magic==0.4.27
python-dotenv==1.2.2
requests==2.34.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3

# ============================================================
# Production server
# ============================================================
gunicorn==26.0.0

# ============================================================
# Chatbot / RAG — Groq (LLM) client
# groq's SDK is httpx-based; these are its actual runtime deps,
# not leftover langchain bloat.
# ============================================================
groq==1.6.0
httpx==0.28.1
httpx-sse==0.4.3
httpcore==1.0.9
h11==0.16.0
anyio==4.14.1
sniffio==1.3.1
distro==1.9.0
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
typing_extensions==4.16.0
typing-inspection==0.4.2

# ============================================================
# Chatbot / RAG — local embeddings (BAAI/bge-small-en-v1.5)
# CPU-only build (see the --extra-index-url line below) — this
# single change is what removes ~4-5GB of CUDA/nvidia packages
# from your original list. Without it, plain `pip install torch`
# pulls the GPU build, which drags in cuda-toolkit, triton, and
# 14 separate nvidia-* packages for hardware this project never
# uses (no GPU anywhere in this stack).
# ============================================================
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
sentence-transformers==5.6.1
transformers==5.14.1
tokenizers==0.22.2
safetensors==0.8.0
huggingface_hub==1.23.0
hf-xet==1.5.1
numpy==2.5.1
scipy==1.18.0
scikit-learn==1.9.0
joblib==1.5.3
threadpoolctl==3.6.0
filelock==3.29.7
fsspec==2026.6.0
mpmath==1.3.0
sympy==1.14.0
networkx==3.6.1
tqdm==4.68.4
regex==2026.7.19

# ============================================================
# Vector search
# ============================================================
pgvector==0.3.6

# ============================================================
# Base HTTP/TLS stack (requests' own deps)
# ============================================================
certifi==2026.6.17
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0

# ============================================================
# Generic low-level utilities other packages above depend on
# ============================================================
packaging==26.2
setuptools==83.0.0
six==1.17.0
attrs==26.1.0
